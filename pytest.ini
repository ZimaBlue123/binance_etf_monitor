[pytest]
# 显式声明测试发现根目录,避免 pytest 在仓库根目录扫不到任何 test_*.py
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning
